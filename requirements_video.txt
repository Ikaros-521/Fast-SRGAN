# Fast-SRGAN 视频推理依赖
# 基础依赖（图像推理）
torch>=1.9.0
torchvision>=0.10.0
pillow>=8.0.0
omegaconf>=2.1.0
tqdm>=4.60.0
numpy>=1.21.0

# 视频推理额外依赖
opencv-python>=4.5.0
psutil>=5.8.0

# 可选依赖（用于更好的性能）
# tensorrt  # NVIDIA TensorRT (需要NVIDIA GPU)
# onnx     # ONNX导出支持
